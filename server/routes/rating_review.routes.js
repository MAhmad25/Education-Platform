const express = require("express");
const router = express.Router();
const ratingReviewController = require("../controllers/rating_review.controller");
// Define rating & review routes here
module.exports = router;
